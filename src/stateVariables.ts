import {Character} from './character.ts';
import { Maps } from './maps.ts';
export const stateVariables = {
    player: {} as Character,
    bgImage: {} as Maps,
    windowWidth: 1366,
    windowHeight: 650,
}